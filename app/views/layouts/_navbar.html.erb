<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/#">
				<%= image_tag("PSLogo.png")%>
			</a>
    </div>
		<div class="navbar-collapse collapse">
			<ul class="nav navbar-nav primary-links">
				<li><a href="/#/discover">Discover</a></li>
				<% if logged_in? %>
				<li><a href="/#/projects/new">Start</a></li>
				<% else %>
				<li><a href="<%= new_session_url %>">Log In to Create a Project!</a></li>
				<% end %>
			</ul>
			<span class="for-search-bar"></span>
			
	    <ul class="nav navbar-nav navbar-right user-links">
	      <% if logged_in? %>
	        <li class="dropdown me-drop-down">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
							Me
							<b class="caret"></b>
						</a>
						<div class="dropdown-menu row">
							<div class="col-xs-6">
									<ul class="my-stuff-links">
										<li><strong>My Stuff</strong></li>
										<li><i class="fa fa-envelope-o"></i>  <a href="/#/oops" class="empty-link">Messages</a></li>
										<li><i class="fa fa-bar-chart-o"></i>  <a href="/#/oops" class="empty-link">Activity</a></li>
										<li><i class="fa fa-star-o"></i>  <a href="/#/oops" class="empty-link">Starred projects</a></li>
									</ul><br>
									<ul class="account-links">
										<li><h5><strong>Account</strong></h5></li>
										<li><i class="fa fa-user"></i>  <a href="<%= user_url(current_user)%>">Profile</a></li>
										<li><i class="fa fa-sitemap"></i>  <a href="/#/oops" class="empty-link">Backer history</a></li>
										<li><i class="fa fa-wrench"></i>  <a href="/#/oops" class="empty-link">Settings</a></li>
										<li><i class="fa fa-users"></i>  <a href="/#/oops" class="empty-link">Find friends</a></li>
									</ul><br>
							</div>
							
							<% if logged_in? %>
								<% if !current_user.created_projects.empty? %>
								<div class="col-xs-6">
									<ul class="project-links">
										<li><h5><strong>Created projects</strong></h5></li>
										<% current_user.created_projects.each do |project| %>
											<li><i class="fa fa-briefcase"></i>  <a href="<%= api_project_url(project)%>"><%= project.title%></a>
											</li>
										<% end %>
									</ul>
								</div>
								<% end %>
							<% end %>
							<div class="logout-btn" style="clear:both"><p> You're logged in as <%= current_user.name%><br> <%= link_to("Log out", session_url, { method: :delete})%></p></div>
						</div>
					</li>
				<% else %>
					<li><a class="btn nav-bar-new" href="<%= new_user_url %>">Sign Up</a></li>
					<li><a class="btn nav-bar-new" href="<%= new_session_url %>">Log In</a></li>
	      <% end %>
	    </ul>
		</div	>
  </div>
</nav>